# Menu & Recipes

- Kitchen Menus: `BSConfig.Menus` (keys, labels, needs, duration, server event/args).
- Recipes: `BSConfig.Recipes` (server-side ingredient checks & removals).
- Cooking times: `BSConfig.CookingTimes` (min/perfect/burn).
- Items map: `BSConfig.ItemsMap` (internal → inventory item names).
